'use strict';

module.exports = function delayItems(wheelz) {
    // rewrite render method
    wheelz.on('scroll', function (instances) {
        if (instances.y.velocity > 0) {
            for (var i = wheelz.childrenLength - 1; i >= 0; i--) {
                wheelz.children[i].style.transform = 'translate3d(0, ' + -(wheelz.childrenLength - i) * instances.y.velocity + 'px, 0)';
            }
        } else {
            for (var _i = wheelz.childrenLength - 1; _i >= 0; _i--) {
                wheelz.children[_i].style.transform = 'translate3d(0, ' + -1 * _i * instances.y.velocity + 'px, 0)';
            }
        }
    });
};